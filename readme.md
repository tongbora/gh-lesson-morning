#Hello

```bash
    # to check your status account
    git auth status
    # to login to github 
    gh auth login

    # to create repo 
    gh repo create <repo name> --public (for public repo)

```